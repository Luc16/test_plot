# Test for ploting with opengl

## Dependencies

- CMake
- OpenGL (To install on Ubuntu run following command)
```bash
apt install libgl1-mesa-dev libglu1-mesa-dev
````

## Build

```bash
mkdir build
cd build
cmake ..
make
```